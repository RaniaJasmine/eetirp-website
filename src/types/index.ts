/* Temporary Debugging Rule - Add this to your global CSS */
* {
    outline: 1px solid rgba(255, 0, 0, 0.1) !important;
}

/* Force everything in the footer to the top layer */
#footer, #footer * {
    pointer- events: auto!important;
}

#services, #services * {
    pointer- events: auto!important;
}